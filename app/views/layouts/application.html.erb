<!DOCTYPE html>
<html>
  <head>
    <title>YT Share</title>
    <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
    <link rel="icon" type="image/png" href="<%= asset_path( 'favicon.png' ) %>" />
    <meta name="viewport" content="width=device-width, initial-scale=1">
  </head>
  <body>

    <header>
      <%= link_to 'Add your favorite video', '#', data: {toggle: 'modal', target: '#myModal'} %>
      <%= link_to 'Newest Video', '#' %>

    </header>

    <!-- Button trigger modal -->
 

  <!-- Modal -->
  <div class="modal fade" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
    <div class="modal-dialog">
      <div class="modal-content">
        <div class="modal-header">
          <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
          <h4 class="modal-title" id="myModalLabel">Add Your Video</h4>
        </div>
        <%= simple_form_for Video.new do |f| %>
          <div class="modal-body">
            <h1>Enter Your Video Details</h1>
            
              <%= f.input :name, label: "Name of Youtube Video", placeholder: "Super Awesome Video!" %>
              <%= f.input :url, label: "Youtube Video URL", placeholder: "https://www.youtube.com/watch?v=dQw4w9WgXcQ" %>
                        
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
            <%= f.submit 'Add Video', class: 'btn btn-danger' %>
          </div>
        <% end %>
        
      </div>
    </div>
  </div>

    <br class="clear" />
    
    <hr />

    <div class="container">
      
      <% if flash[:error].present? %>
        <div class="col-xs-10 col-xs-offset-1 alert alert-danger">
          <%= flash[:error].html_safe %>
        </div>
      <% end %>

      <%= yield %>
    </div>

    <div class="footer">
      <div class="container">
        <div class="col-xs-12 col-sm-6">
          
        </div>
        <div class="col-xs-12 col-sm-6 footer-blurb">
          
        </div>
      </div>

  </body>

</html>
